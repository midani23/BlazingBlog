@using blazingBlog.Domain.Articles

<div class="article-card">
    <h4>@Article.Title</h4>
    <p>@Article.Content</p>
    <small>@Article.DatePublished.ToString("g")</small>
</div>

@code {
    [Parameter]
    public required Article Article { get; set; }
}